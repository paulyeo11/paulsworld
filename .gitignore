.vercel
node_modules/
package-lock.json
package.json

# Local-only IBKR refresh tooling (not part of the public website)
refresh_t20.py
refresh-t20.bat
make-shortcut.ps1
IBKR-REFRESH-SETUP.txt

add-ibgw-startup.ps1

# Tiger Brokers secrets — NEVER commit these
tiger_private_key.pem
tiger_config.txt
tiger_private_key.pem.bak

# Tiger auto-refresh local tooling (not part of the public website)
refresh_tiger.log
